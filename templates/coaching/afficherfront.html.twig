{% extends 'baseEmna.html.twig' %}

{% block title %}Coaching{% endblock %}
{% block topheader %}
{% include 'test.html.twig' %}

{% endblock %}

{% block body %}
<link rel="stylesheet" href="{{ asset('bundles/mercuryseriesflashy/css/flashy.css') }}">


		<!-- /SECTION -->
<div class="section">
			<!-- container -->
			<div class="container">
				<!-- row -->
				<div class="row">
				{%for i in Coaching%}
					<!-- shop -->
					<div class="col-md-4 col-xs-6">
						<div class="shop">
							<div class="shop-img">
								<img src="{{ asset('/uploads/coach/' ~ i.imgCoach) }}?t={{ date().getTimestamp() }}" alt="image"  width = "5%">
							</div>
							<div class="shop-body">
								<h3>{{i.Cours}}</h3>
								<a href="{{ path('detailcoach', {id:i.id}) }}" class="cta-btn"> Détailles <i class="fa fa-arrow-circle-right"></i></a>

							</div>
						</div>
					</div>
					{%endfor%}
		</div>
		</div>
		</div>


{% endblock %}







