{% extends 'baseBack.html.twig' %}
{% block body %}
<div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title">Basic Table</h4>
                    <p class="card-description"> table<code>des types</code>
                    </p>
                    <div class="table-responsive">
                      <table class="table">
                        <thead>
                          <tr>
                            
                            <th>Coaching</th>
                            <th>Date</th>                          
                            
                          </tr>
                        </thead>
                        <tbody>
                        {% for a in rdv %}
                          <tr>
                            <td>{{a.id }}</td>
                            <td> {{a.coachings}} </td>
                            <td> {{a.DateRdv.format('j-m-y') }} </td>
                            <td><a href="{{ path('updaterdv', {id:a.id}) }}" class="btn btn-primary" >Traiter</a></td>

                          </tr>
                          {% endfor %}
                        </tbody>
                      </table>
                    </div>
                  </div>
                </div>
                
              
{% endblock %}