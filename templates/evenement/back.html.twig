{% extends 'baseback.html.twig' %}

{% block body %}
<div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                  <div class="card-body">
                    
                    <p class="card-description"> table<code>des types</code>
                    </p>
                    <div class="table-responsive">
                      <table class="table">
                        <thead>
                          <tr>
                            <th>identifiant</th>
                            <th>Nom event</th>
                            <th>date debut</th>
                            <th>date fin</th>
                            <th>lieu event</th>
                            <th>Description</th>
                            <th>Type</th>
                            <th>Notre Sponsor</th>
                            
                          </tr>
                        </thead>
                        <tbody>
                        
                                                      {% for i in Evenement %}

                                            <tr>
                                                <td>
                                                    {{i.ID}}
                                                </td>
                                                <td>
                                                   {{i.Nom}}
                                                </td>
                                               
                                                <td>
                                                    {{ i.DateDebut|date('Y-m-d H:i:s')  }}
                                                </td>
                                                <td>
                                                    {{ i.DateFin|date('Y-m-d H:i:s')  }}
                                                </td>
                                              <td>
                                                    {{i.Lieu}}
                                                </td>
                                                 <td>
                                                    {{i.Description}}
                                                </td>
                                                <td>
                                                    {{i.Type}}
                                                </td>
                                                 <td>
                                                    {{i.Sponsors}}
                                                </td>
                                                 <td>
                                                    <a href="{{ path('deleteEvenement', {id:i.ID}) }}">Delete </a>
                                                </td>
                                                 <td>
                                                    <a href="{{ path('updateEvenement', {id:i.ID}) }}">Update </a>
                                                </td>
                          </tr>
                          {% endfor %}
                        </tbody>
                      </table>
                    </div>
                  </div>
                </div>
                
              
{% endblock %}