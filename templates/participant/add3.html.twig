{% extends 'baseEmna.html.twig' %}
{% block title %}Participant{% endblock %}
{% block topheader %}
{% include 'test.html.twig' %}

{% endblock %}

{% block body %}
 {%  for message in app.flashes('error')  %}
                               <div class="alert alert-info" role="alert">
                                   {{message}}                               </div>
                           {% endfor %}
<!-- SECTION -->
		<div class="section">
			<!-- container -->
			<div class="container">
				<!-- row -->
				<div class="row">

					<div class="col-md-7">
						<!-- Billing Details -->
                        
						<div class="billing-details">
							<div class="section-title">
								<h3 class="title">Participer Maintenant !</h3>
							</div>
                            
							  {{ form_start(formParticipant , {'attr': {'novalidate':'novalidate'}} ) }}
                    
                        {{ form_row(formParticipant.nom, {'label':'Votre nom' ,'attr':{'placeholder': 'nom...', 'class':'input'}}) }}
                        {{ form_row(formParticipant.prenom, {'label':'Votre prenom' ,'attr':{'placeholder': 'prenom...', 'class':'input'}}) }}
					
                        {{ form_row(formParticipant.email, {'label':'Votre email' ,'attr':{'placeholder': 'email...', 'class':'input'}}) }}
                        {{ form_row(formParticipant.age, {'label':'Age' ,'attr':{'placeholder': 'age...', 'class':'input'}}) }}

                        {{ form_row(formParticipant.tel, {'label':'tel' ,'attr':{'placeholder': '+216...', 'class':'input'}}) }}
					
                        {{ form_row(formParticipant.evenement, {'label':'Les evenements disponibles :' ,'attr':{'placeholder': 'tel...', 'class':'input'}}) }}
					
          {{form_widget(formParticipant.captcha) }}
                      <button type="click here" class="btn btn-primary mr-2" >Participer</button>
                       {{form_end(formParticipant)}}
                     
							
					
                      
						<!-- /Billing Details -->
              </div>
              </div>
              </div>
              </div>
            {% endblock %}


{% block footer %}

{% endblock %}            