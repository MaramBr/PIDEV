{% extends 'baseback.html.twig' %}

{% block body %}
<div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                  <div class="card-body">
                    <h4 class="card-title">Basic Table</h4>
                    <p class="card-description"> table<code>des types</code>
                    </p>
                    <div class="table-responsive">
                      <table class="table">
                        <thead>
                          <tr>
                            <th>id</th>
                            <th>email</th>
                            <th>nom</th>
                            <th>prenom</th>
                            <th>image</th>
                             <th>password</th>

                           <th>roles</th>
                                                      <th>etat</th>

                                                      <th>Modifier</th>
                                                      <th>Supprimer</th>

                            <th>Activer</th>
                            <th>Bloquer</th>





                            
                          </tr>
                        </thead>
                        <tbody>
                        {% for f in user %}
                          <tr>
                            <td>{{f.id }}</td>
                             <td> {{f.email }} </td>
                            <td> {{f.nom }} </td>
                              <td> {{f.prenom }} </td>
                             <td> <img src="/uploads/{{ f.Image }}" alt="image event" width="100" height="100"> </td>
                                <td> {{f.password }} </td>
<td>
{% for unrole in f.roles %}
<li>{{unrole}}</li>
{% endfor %} 
</td>
                                <td> {{f.isActive }} </td>


                            <td><a href="utilisateur/update/{{f.id}}" class="btn btn-primary mr-2" >modifier</a></td>
                            <td><a href="utilisateur/delete/{{f.id}}" class="btn btn-dark" >supprimer</a></td>
                             <td><a href="/user/{{f.id}}/enable" class="btn btn-success" >Activer</a></td>
                          <td><a href="/user/{{f.id}}/disable" class="btn btn-danger" >Bloquer</a></td>
                          </tr>
                          {% endfor %}
                        </tbody>

                         <a href="{{ path('app_registerback') }}" class="btn btn-success" >
                                +Add
         
                             <a href="/stat3" class="btn btn-success" >
                                Stat
                            </a>  
                                        <a  id="createbuttonDropdown" data-toggle="dropdown" aria-expanded="false" href="#">+ Create New Project</a>

                      </table>

                      <nav aria-label="Page navigation example">
                              <ul class="pagination">
								
								        <li class="page-item"><a href={{ knp_pagination_render(user) }}</a></li>
						
						                    	</ul>
						                    </nav> 
                      </center>
                    </div>
                  </div>
                </div>
                
              
{% endblock %}